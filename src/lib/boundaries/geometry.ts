// Rich-mark geometry minimums in screenshot-natural CSS px (VAL-MARK-002).

/** Minimum width/height of a rectangle or circle (circles stay square). */
export const MIN_SHAPE_SIZE_PX = 8;

/** Minimum distance between an arrow's start and end points. */
export const MIN_ARROW_LENGTH_PX = 16;
